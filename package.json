{
  "name": "willie-aquasys-proxy",
  "version": "1.0.0",
  "description": "Serverless proxy sécurisé entre Aquasys et l'API Willie, avec filtrage par compteurs autorisés.",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start:local": "npm run build && node scripts/local-server.mjs",
    "zip": "npm run build && rm -f willie-aquasys-proxy.zip && zip -r willie-aquasys-proxy.zip package.json dist README.md .env.example"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "typescript": "^5.6.0"
  }
}
